import { Button } from "./components/button/Button";

function App() {
  return (
    <section className="center-container">
      <Button label="Weitere informationen" onClick={() => {}} primary />
    </section>
  );
}

export default App;
